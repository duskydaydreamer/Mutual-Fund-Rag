# Run the Mutual Fund FAQ frontend locally

1. Confirm the current workspace is the `RAG/frontend` folder.
2. If dependencies are missing, run `npm ci`.
3. Start the development server with `npm run dev:local`.
4. Wait for the Local URL and verify `http://localhost:3000` loads successfully.
5. Keep the development server running and report the local URL to the user.
